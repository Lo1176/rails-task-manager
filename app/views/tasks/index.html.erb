<h1>Here is all the tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <% if task.completed %>
    <input type="checkbox" checked>
    <% else %>
    <input type="checkbox">
    <% end %>
    <li><%= link_to task.title, task_path(task) %></li>
    <li><%= link_to "🗑",  task_path(task), method: :delete %></li>
  <% end %>
</ul>


<%= link_to "+ Add a new task",  tasks_new_path %>
